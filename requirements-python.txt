# EGX Python — dual-path install (cron /usr/bin/python3 + dev pyenv)
# macOS lightgbm: brew install libomp
#
# Install both paths:
#   bash scripts/install_python_deps.sh
#   npm run egx:python:install

# ── Analytics / data layer ────────────────────────────────────────────────────
duckdb>=0.10.0
pyarrow>=14.0.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0; python_version < "3.11"
numpy>=1.24.0; python_version >= "3.11"

# ── ML core ───────────────────────────────────────────────────────────────────
lightgbm>=4.0.0
xgboost>=1.7.0
scikit-learn>=1.3.0
scipy>=1.10.0
joblib>=1.3.0
optuna>=3.0.0
shap>=0.42.0
lifelines>=0.30.0

# ── Stats / feature engineering ───────────────────────────────────────────────
statsmodels>=0.14.0
tsfresh>=0.20.0

# ── MLOps / market data ───────────────────────────────────────────────────────
mlflow>=2.0.0
yfinance>=0.2.0

# ── Graph / causal (optional but used by discovery engines) ───────────────────
networkx>=3.0

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.28.0
PyYAML>=6.0
python-dotenv>=1.0.0
matplotlib>=3.7.0
